import { motion } from 'framer-motion'
import styled from 'styled-components'

export const ErrorStyled = styled(motion.div)`
  border-radius: 30px;
  padding: 5px 15px;
  color: var(--error);
  align-self: start;
  /* background-color: #a21e1e3c; */
`
